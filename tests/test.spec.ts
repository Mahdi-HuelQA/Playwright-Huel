// import { test, expect } from '@playwright/test';

// test('Example test with WebKit', async ({ playwright }) => {
//   // Launch the WebKit browser
//   const browser = await playwright.webkit.launch();

//   // Create a new page
//   const page = await browser.newPage();

//   // Navigate to the example.com website
//   await page.goto('https://uk.huel.com');

//   // Perform other actions on the page
//   // For example, you can check the page title
//   const title = await page.title();
//   await expect(title).toBe('Example Domain');

//   // Close the browser
//   await browser.close();
// });

//practice script
